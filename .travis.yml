language: cpp
compiler:
  - gcc
  - clang
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install libboost-all-dev
before_script:
  - mkdir build
  - cd build
  - cmake .. -DKunlaboro_BOOST=1
script:
  - make -j4